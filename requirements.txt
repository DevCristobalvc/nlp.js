# Python 3.12.7

# Flask para la creación de aplicaciones web simples
Flask==2.2.3

# Librería para realizar solicitudes HTTP
requests==2.32.3

# pdfplumber para extraer texto de archivos PDF
pdfplumber==0.8.0

# pandas para manipulación de datos (puede ser útil si necesitas procesar tablas u organizar datos)
pandas==2.2.2

# sentence-transformers para generar embeddings de texto
sentence-transformers==3.3.0

# transformers para trabajar con modelos de lenguaje de Hugging Face
transformers==4.46.2

# torch para utilizar modelos de aprendizaje profundo (necesario para sentence-transformers y transformers)
torch==2.5.1

# torchvision para manejar funciones de visión (si es necesario trabajar con imágenes)
torchvision==0.19.0

# tqdm para mostrar barras de progreso en la terminal (útil para loops largos)
tqdm==4.67.0

# NumPy, una dependencia de muchas librerías para operaciones matemáticas y manejo de arrays
numpy==2.1.3

# tokenizers para el procesamiento de texto (necesario para transformers)
tokenizers==0.20.3

# scikit-learn para funcionalidades adicionales de procesamiento de datos (si es necesario)
scikit-learn==1.5.2

# huggingface-hub para descargar modelos desde el hub de Hugging Face
huggingface-hub==0.26.2

# rapidfuzz para la similitud de texto (opcional)
rapidfuzz==3.9.4
